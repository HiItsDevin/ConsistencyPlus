package io.github.consistencyplus.consistency_plus.base

import net.minecraft.block.CarvedPumpkinBlock

class BaseConsistencyCarvedPumpkinBlock(settings: Settings?) // I hate that I have to do this.
    : CarvedPumpkinBlock(settings)
